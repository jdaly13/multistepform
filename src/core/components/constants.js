export const stepMapping = Object.freeze({
  from: "from",
  to: "to",
  confirm: "confirm",
  weight: "weight",
  shipping: "shipping"
});

export const shippingOptionObj = Object.freeze({
  ground: 1,
  priority: 2
})
